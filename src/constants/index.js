export const PLACEHOLDER_TYPES = {
  INPUT: 'input',
  OUTPUT: 'output'
};

export const EXPRESSION_REGEX = /^[a-zA-Z0-9+\-*/^().\s_]+$/;

export const VALID_PATTERN = /^[-+*/^0-9x().\s]*$/;
export const OPERATOR_PATTERN = /[+\-*/^]$/;
